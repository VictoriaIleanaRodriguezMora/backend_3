# 07 - Nest/ts

[PPT](https://docs.google.com/presentation/d/1Lxy51FGXhnELJt4LdMj5DekS-yMd5h3ISRbgBQluWyo/edit?slide=id.g120b44b0dae_0_606#slide=id.g120b44b0dae_0_606)

```bash
nest new app-nest-mongo --skip-git
```

# AppModule

La app se crea a partir de AppModule

# @Decoradores

Métodos/funciones que se invocan con el @nombre decorador
Nest ya tiene la funcionalidad definida para cada uno de estos decoradores

## imports - módulos del proyecto

- de usuarios
- de mascotas

# Generar un módulo

```bash
nest generate resource User
# ✔ What transport layer do you use? REST API
# ✔ Would you like to generate CRUD entry points? Yes
```

## El módulo creado se agrega en el array de imports de `app.module.ts` con el comando

```ts
import { Module } from "@nestjs/common";
import { AppController } from "./app.controller";
import { AppService } from "./app.service";
import { UserModule } from "./user/user.module";

@Module({
  imports: [UserModule],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

### `user.module.ts`

Acá, no tengo un array de imports, PERO PODRÍA
Podría ser que este modulo, llame a otros dentro de sí

### `07_nest/app-nest/src/user/entities/user.entity.ts`

Configuro los tipos de datos de mi entidad

```ts
export class User {
  id: number;
  first_name: string;
  last_name: string;
  nick_name?: string; // digo que es opcional con el ?
}
```

### `07_nest\app-nest\src\user\dto\create-user.dto.ts`

Sin el id del entity

```ts
export class CreateUserDto {
  first_name: string;
  last_name: string;
  nick_name?: string; // digo que es opcional con el ?
}
```

### `07_nest/app-nest/src/user/user.service.ts`

```ts
import { Injectable } from "@nestjs/common";
// import { CreateUserDto } from './dto/create-user.dto';
// import { UpdateUserDto } from './dto/update-user.dto';
import { User } from "./entities/user.entity";

@Injectable()
export class UserService {
  users: User[];
  create(user: User): User {
    this.users.push(user);
    return user;
  }

  findAll(): User[] {
    return this.users;
  }

  findOne(id: number): User | null {
    const user = this.users.find((prd) => prd.id === id);
    if (!user) return null;
    return user;
  }
}
```

💡Finalizar todos los servicios ligados al controlador para tener una funcionalidad completa con el módulo de usuarios.
👀try/catch en controller
01:48:00

# MongoDB + Nest

Nest proporciona un módulo interno para poder utilizar mongoose

```bash
npm i @nestjs/mongoose mongoose
```

```ts

```

## Para hacer uso de .env

```bash
npm i @nestjs/config
```

# Módulo de usuarios

```bash
nest g resource user --no-spec
```

# `app.module`

```ts
import { Module } from "@nestjs/common";
import { AppController } from "./app.controller";
import { AppService } from "./app.service";
import { MongooseModule } from "@nestjs/mongoose";
import { ConfigModule, ConfigService } from "@nestjs/config";
import { UserModule } from "./user/user.module";

@Module({
  imports: [
    ConfigModule.forRoot(),
    UserModule,
    MongooseModule.forRoot("mongodb://localhost:27017/bdd"),
  ],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

## Schema de Usuarios

⬇️ Crear el archivo `src/user/schema/user.schema.ts`

```ts
import { Schema, Prop, SchemaFactory } from "@nestjs/mongoose";
import { Document } from "mongoose";

@Schema() // me habilita la creacion del schema
export class User {
  @Prop({ required: true }) // acá van las configuraciones de cada propiedad
  first_name: string;
  @Prop({ required: true })
  last_name: string;
  @Prop({ required: true, unique: true })
  email: string;
  @Prop({ required: true })
  role: string;
}

// le digo que me genere el schema a partir de la funcionalidad SchemFactory
export const UserSchema = SchemaFactory.createForClass(User);
export type UserDocument = User & Document; // creo mi TIPO. User y Document de Mongo
```

⬇️ `src/user/user.module.ts`

```ts
import { Module } from "@nestjs/common";
import { UserService } from "./user.service";
import { UserController } from "./user.controller";
import { MongooseModule } from "@nestjs/mongoose";
import { User } from "./schema/user.schema";
import { UserSchema } from "./schema/user.schema";

@Module({
  imports: [
    MongooseModule.forFeature([
      {
        name: User.name, // nombre de la collection
        schema: UserSchema,
      },
    ]),
  ], // agrego esto
  controllers: [UserController],
  providers: [UserService],
})
export class UserModule {}
```

## Modificando los métodos del Servicio

### Carpeta utils - hasheo

```bash
npm i bcryptjs @types/bcryptjs
```

⬇️ `src/users/utils/utils.ts`

```ts
// Con esto, voy al service
import { compareSync, hashSync } from "bcryptjs";
import { CreateUserDto } from "../dto/create-user.dto";

export const createHash = (password: string) => hashSync(password, 10); // cantidad de veces que recorre el string para generar el hash

export const isValidPassword = (password: string, user: CreateUserDto) =>
  compareSync(password, user.password);
```

⬇️ `src/user/user.service.ts`

```ts

```

⬇️ `src/user/dto/create-uset.dto.ts`

```ts
// es el objeto que envío por Body para registrar el usuario
export class CreateUserDto {
  first_name: string;
  last_name: string;
  email: string;
  role?: string;
  password: string;
  age: number;
}
```

# Entornos

00:37:00 - jwt

⬇️ ``

```ts

```

```bash

```
