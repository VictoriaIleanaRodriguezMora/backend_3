# 05 - Seguridad y documentación

[PPT](https://docs.google.com/presentation/d/13pXUd0uGHek8fI6TQ7ddgZxX3RxphAq-gbHblmY6J-Q/edit?slide=id.g120b44b0dae_0_606#slide=id.g120b44b0dae_0_606)

# Seguridad

Riesgos mas comunes en nuestras aplicaciones y normas de seguridad.

hardware, físico, servidor. Pueden

# OWASP - https://owasp.org/

Entidad sin fines de lucro, que intenta que las aplicaciones se desarrollen de la manera más segura posible.
se reunen y auditan aplicaciones con los riesgos más comunes con los que se pueden encontrar y como se pueden enfrentar
desarrollan herramientas para auditar redes, publican libros, dan charlas.
son una comunidad, una fundacion.



la idea es no solo ser conscientes de cómo escribimos el código, cómo perfeccionar nuestra aplicacion, lograr un buen rendimiento, sino que tambien tener en cuenta la seguridad, algo que por ahi no tenemos tan en cuenta, ni tocamos tanto. 

porque bueno, la ciberseguridad es todo un mundo aparte, se puede ser un experto en ciberseguridad, cosa que no va a pasar hoy 😂. 

uno es un experto en saber los mecanismos en los que un usuario malintencionado puede acceder en forma no autorizada a recursos del sistema, en general son personas expertas en la materia, por lo que el conocimiento que ellos tengan va a ser avanzados que los nuestros

estas organizaciones tratan de concientizar sobre estos peligros a los que estamos expuestos


## Seguridad y documentación

### El temor de todo desarrollador…

Si has llegado hasta este curso avanzado, seguramente tienes los conocimientos para desarrollar un servidor backend desde cero mediante buenas prácticas.

Sin embargo, hay algo más de lo que hay que hablar sobre el desarrollo, además de las arquitecturas, testing y escalabilidad: **las vulnerabilidades**.

Me imagino que en más de una ocasión habrás escuchado hablar sobre el hackeo de X plataforma, sobre el robo de información de X base de datos, o la simple caída de X página web debido a alguna persona malintencionada.

Cuando nuestro servidor se encuentre arriba, siempre tendremos este pequeño cosquilleo:

- “¿Y si en algún momento, a causa de mi sistema web, alguien logra extraer información de mis usuarios?”
- “No quiero que sea mi página web la que comprometa la información sensible de mis clientes”

---

### ¿Qué es una vulnerabilidad?

En el mundo IT, una **vulnerabilidad** es cualquier tipo de debilidad o brecha dejada en nuestro sistema, que permita a una persona malintencionada aprovecharla para comprometer la seguridad del sistema o el usuario.

Existen múltiples tipos de vulnerabilidades:

- **De hardware**: cuando la implicación de seguridad se da en un elemento físico (como el servidor).
- **De software**: cuando la implicación de seguridad se da en nuestro aplicativo.
- **Procedimentales**: cuando la implicación de seguridad se da en el proceso mismo de resolución.
- **Humanas**: cuando la implicación de seguridad se da en una persona o usuario.

Cuando una persona malintencionada encuentra alguna de estas vulnerabilidades, puede tomar acción para **explotarla** y convertirlo en un evento preocupante.

---

## OWASP

### ¿Qué es OWASP?

**OWASP** (Open Web Application Security Project) es un proyecto de código abierto internacional, sin fines de lucro, que brinda información referente a la seguridad general de aplicaciones web.

OWASP provee un conjunto de herramientas que permiten conocer a profundidad conceptos y herramientas de seguridad:

- **OWASP ZAP**: Proxy para hacer testing de peticiones entre el navegador y aplicaciones.

https://owasp.org/search/?searchString=zap

- **OWASP Juice Shop**: Aplicativo de prueba con vulnerabilidades, donde existen retos de búsqueda y explotación.
- **OWASP Testing Guide**: Guía sobre cómo testear tu aplicación web, con un checklist de vulnerabilidades.

---

### OWASP Top 10

Es un documento que muestra las **10 vulnerabilidades principales** y de impacto más crítico en las aplicaciones web a lo largo de un determinado tiempo. Se actualiza cada 3-4 años.

---

## Recomendación de canal que habla de ciberseguridad
- https://www.youtube.com/@aprendiendosobrecibersegur9375

#### A01:2021 – Broken Access Control (Control de acceso roto/pérdida de control de acceso)
es la forma en cómo se limita el acceso a funcionalidades o recursos de una aplicación
Se produce cuando se logra acceder a un recurso al cual no se debería tener acceso.

### controles de acceso

**controles de acceso verticales**, restringen el acceso a funcionalidades sensibles, no disponibles a otros usuarios. 

> Ej: Un usuario estandar accede a determinados recursos, pero conoce la url del panel del administrador? 
> Si existe esta falla, el usuario estandar podría acceder al panel administrativo, por url, si la url no tiene protección 

**controles de acceso horizontales**, restringen el acceso a recursos, a los usuarios que están autorizados. pero por ej, un usuario podría tener acceso a los datos de otro usuario.

> Cuando expongo un id, por url le estoy dando un dato, una herramienta para que el pueda modificarlo, y ver la info de otro usuario, que no es él.
> imaginemos que en un panel, con tu id, ver tus datos, tu salario, y solo cambiando el id, un empleado, un compañero pudiera ver cuanto ganamos nosotros.

la mejor forma de evitarlo es no pensar que NADIE lo va a hacer. quien querria entrar a mi app para romperla? quien cambiaria el id de la url? NO pensar esas cosas. evitarlas

eso se puede evitar con middlewares, ver si está logueado, ver si sus credenciales son de admin

! hay que tener cuidado con los tokens, que si el usuario se desloguea, no quede ese token vivo. y que no haya posibilidad de que lo robe y reutilice 

**Aplica cuando:**

- Se fuerza una URL para realizar búsquedas a otros recursos.
- Un usuario puede seguir navegando aún cuando su token JWT haya expirado.
- Ruptura de una cookie con un token para elevar privilegios.

---

#### A02:2021 – Cryptographic Failures (Fallos criptográficos/exposición de datos sensibles)

> Ej. No encriptar la contraseña
Y aunque esté encriptada, hay algunos algoritmos o funciones de hasheo que ya no son recomendadas.

**Librería crypto** --> ya no se recomienda por seguridad
! Antes de utilizar un algoritmo de hasheo buscar,  investigar si son recomendados o no.

Uso descuidado de procesos criptográficos, ya sea por aplicación errónea o por algoritmos obsoletos.

**Aplica cuando:**

**cookies** al manejar tokens 
> Agregarle la opción de `{http-only: true}`, para que desde http, no puedan acceder a las cookies

**headers** / encabezados, seguros 
existen headers que se configuran para envío de datos muy sensibles

> Strict-Transport-Security - exige el uso de **https**, para el intercambio de información
###### http y https
son protocolos de transferencia de hipertexto, sólo que **https** es seguro, la información se envía de manera más segura. Lo que hace es encriptar los datos, entre cliente y servidor 
- https://developer.mozilla.org/es/docs/Web/HTTP/Reference/Headers/Strict-Transport-Security

- Se utiliza algún algoritmo de cifrado obsoleto.
- El proceso de hasheo es intervenido directamente por el desarrollador, generando claves poco sólidas.
- La librería externa que utilizamos no se actualiza correctamente (relacionado con A06).


! Los datos sensibles, se manejan en lo que se llaman fronteras. Hay **fronteras confiables**, que es cuando guardamos datos encriptados. 
> Típico caso, la contraseña. Que se guarda en la bdd, ya encriptada, hasheada

**frontera NO confiable**, qué es cuando se implementa el cifrado no en almacenamiento sino en transito.
> Ej admito pagos con tarjeta de credito/debito. Y que los datos viajen pelados, asi nomas, del front al servidor.
Eso es un problema, porque si alguien interfiere en el medio y esos datos se estan mandando por body, se está poniendo en riesgo la tarjeta, los datos del usuario 

Ahí entra en juego el cifrado en transito que, lo que hace es cifrar los datos antes de enviarlos desde el front y sólo el back puede descifrarlos

Si estoy usando una librería de terceros, debo verificar si tiene vulnerabilidades. Sí es segura, si tiene muchas descargar, buscar opiniones. 

---

#### A03:2021 – Injection (Inyección)

Es una vulnerabilidad que permite interferir en las consultas que hace la aplicacion a su bdd, puede modificar, borrar esos datos y modificar el comportamiento de la app.

Puede suceder porque los datos del usuario no son validados.
> Ej: Un usuario llena un formulario, y los datos son guardados, tal cual el usuario ingresó.
En ese formulario, pueden enviar una sentencia SQL y borrar una bdd, si no se valida el formato en el que llega 

La inyección en bdd no relacionales es menos frecuente, pero puede suceder tambien. con mongoose hay una capa más de seguridad, porque esta vulnerabilidad sucede en gral cuando la bdd relacional no tiene un tipadp. con mongoose hay que especificar si o si un tipo de dato. sin querer es una validacion que estamos aplicando

> El profe que tuve de backend, nos contó que hace 30 años, el estaba de Jr BackEnd en una empresa. Y que le habian asignado una tarea. Y él, la empezó y creó un bucle infinito, que quedó corriendo todo un fin de semana, haciendo consultas consultas a la bdd. Y el Lunes se toparon con que el uso de la bdd estaba excedido, que ya no tenian recursos para tener la bdd en funcionamiento. Pensaron que habian sido atacados, que alguien los habia vulnerado y usado su sistema para algo, que usaron los recursos, las configuraciones con la bdd. Y no... fue él, que dejó corriendo un bucle que hizo consultas durante mas de 2 días sin supervision, sin un limite.

En ese momento no se hablaba de escalamiento ni vertical ni horizontal, no como hoy. Y menos una pyme pequeña de Argentina.

la inyeccion de js sucede cuando por ej el usuario en lugar de poner su nombre escribe un codigo js. entonces como nosotros no esperamos que el usuario envíe eso, confiamos en que el usuario, no lo haga, la aplicación va a procesar ese código y se va a ejecutar. el atacante lo que hace es ejecutar codigo js en nuestra aplicacion, de manera insegura para nosotros.

es como irse de casa dejando la puerta abierta, creyendo que nadie va a entrar. puede o no pasar. 

Inserción de información por parte del usuario con el fin de romper alguna consulta u obtener información sensible.

**Aplica cuando:**

- La data del usuario no fue validada correctamente (ej. `req.body` sin destructurar o validar).
- Un query dinámico sin validación contextual.
- Parámetros malintencionados para realizar consultas a la base de datos (SQL, NoSQL, etc.).

---

#### A04:2021 – Insecure Design (Diseño inseguro)

Conjunto de malas prácticas al momento de construir un aplicativo, desde pequeños detalles hasta elementos estructurales complejos.

**Aplica cuando:**

- Se deja un campo de password en texto plano, sin ocultar.
- Se permite agregar stock con valor `-100`.
- No se aplican patrones de diseño correctamente, generando fuerte acoplamiento.

---

#### A05:2021 – Security Misconfiguration (Malas configuraciones de seguridad)

Descuidos al configurar el aplicativo, módulos o servicios en la nube.

**Aplica cuando:**

- Configuramos erróneamente elementos de arranque (variables de entorno, argumentos).
- Implementamos permisos o características desmedidas (ej. cluster de MongoDB Atlas con IP `0.0.0.0/0`).
- Configuramos mal un cloud service (bucket de AWS S3, ruta de multer).
- Mostramos el stack trace al usuario por mal manejo de errores.

---

#### A06:2021 – Vulnerable and Outdated Components (Componentes vulnerables y obsoletos)

Un componente (externo o propio) se vuelve obsoleto o mantiene una vulnerabilidad no subsanada.

**Aplica cuando:**

- No actualizamos librerías con vulnerabilidades reportadas.
- Dejamos módulos con documentación obsoleta o features sin implementar.
- Actualizamos un módulo sin probar si resolvía la vulnerabilidad inicial.

---

#### A07:2021 – Identification and Authentication Failures (Fallos en identificación y autenticación)

Problemas en el proceso de autenticación que permiten forzar accesos o acceder a información mal tratada.

**Aplica cuando:**

- El sistema de login permite rupturas por fuerza bruta.
- Guardamos contraseñas sin hashear.
- El sistema de “contraseña olvidada” recuerda la contraseña en lugar de restablecerla (la página tenía conocimiento de la contraseña todo el tiempo).

---

#### A08:2021 – Software and Data Integrity Failures (Fallos de integridad de software y datos)

Uso desmedido de módulos, librerías o integraciones de fuentes no confiables, o que presentan alguna vulnerabilidad.

**Aplica cuando:**

- Una dependencia o integración presenta una falla, dejándonos vulnerables también.
- Dependencias que actualizamos sin revisar correctamente (caso Faker.js).
- Procesos CI/CD vulnerados (ej. Heroku + Github con vulnerabilidad de Travis-CI).

---

#### A09:2021 – Logging and Monitoring Failures (Fallos de registro y monitoreo)

No tenemos una correcta gestión de logs y monitoreo del aplicativo.

**Aplica cuando:**

- No hay sistema de monitoreo de APIs para rastrear actividad sospechosa.
- Logs demasiado genéricos que no dan información suficiente para rastrear un error.
- Logs demasiado explícitos que muestran stack trace incluso en producción.
- Logs solo locales sin posibilidad de exportación o filtrado.

---

#### A10:2021 – SSRF (Server Side Request Forgery)

La aplicación no valida la URL que envía un usuario al acceder a un recurso remoto.

**Aplica cuando:**

- El servidor no tiene control de redirecciones o restricción de URL por expresión regular.
- Existe un sistema interno de callbacks que puede desembocar en redirecciones a recursos adicionales.

---

### ¿Qué hago con esta información?

El mundo de la seguridad es mucho más amplio que este Top 10. Sin embargo, como desarrolladores podemos aportar un **grano de arena** cultivándonos en buenas prácticas e implementándolas en nuestros aplicativos.

Para practicar y mejorar tu expertise en seguridad de aplicaciones web, puedes utilizar los proyectos gratuitos de OWASP.

---

☕ **Break** – 10 minutos

---

## Maratón de detección de vulnerabilidades

**¡Es tu turno de ser el analista de vulnerabilidades!**

Te enfrentarás a tres proyectos. Todos resuelven el mismo problema, pero aumentan en complejidad. Deberás analizar el código y la implementación para encontrar vulnerabilidades, clasificarlas según el OWASP Top 10 y luego discutirlas en grupo.

### ¿Cómo escalan los proyectos?

- **Proyecto 1**: Ruteo, controladores, registro de usuario.
- **Proyecto 2**: Ruteo, controladores, registro de usuario, bases de datos, login de usuario.
- **Proyecto 3**: Ruteo, controladores, registro de usuario, login de usuario, vista de perfil, manejo de sesión, bases de datos, variables de entorno, permisos.

---

### #FindTheBug (1)

**Duración:** 10 minutos (5 individual + 5 grupal)  
Clona el proyecto de aquí (carpeta: `proyecto1`)

**Principales vulnerabilidades encontradas:**

- **A05 - Security Misconfiguration**: El puerto está hardcodeado.
- **A04 - Insecure Design**: El input de password no tiene `type="password"`, se puede visualizar.
- **A04 - Insecure Design**: No se valida el input al guardar usuario, se puede guardar vacío.
- **A04 - Insecure Design**: El patrón por capas no se aplica correctamente; el router de sesión tiene funcionalidad directa sin controller.
- **A06 - Vulnerable and Outdated Components**: Módulo de FileSystem para usuarios dejado a medio implementar (feature obsoleta).
- **A07 - Identification and Authentication Failures**: El password no se hashea (a pesar de tener bcrypt instalado).

---

### #FindTheBug (2)

**Duración:** 15 minutos (10 individual + 5 grupal)  
Clona el proyecto de aquí (carpeta: `proyecto2`)

**Principales vulnerabilidades encontradas:**

- **A05 - Security Misconfiguration**: Puerto hardcodeado.
- **A05 - Security Misconfiguration**: URL de MongoDB hardcodeada.
- **A04 - Insecure Design**: No se valida correo repetido al registrar usuario.
- **A04 - Insecure Design**: Input de password sin `type="password"`.
- **A09 - Logging and Monitoring Failures**: Al intentar registrar dos usuarios con el mismo correo, la base de datos devuelve error, pero el log es genérico (solo imprime "error").
- **A05 - Security Misconfiguration**: Manejo de errores mal implementado; al enviar un campo vacío en registro, la alerta devuelve success aunque haya error.

> **Nota adicional:** El Dao de Mongo y el Dao de FileSystem no tienen relación en el nombre del método. Si se hizo una migración de FileSystem a Mongo, el Dao de FileSystem es un **A06 - Outdated component**. Si la intención es usar ambos, es un **A04 - Insecure design** por mal implementación del patrón de persistencia.

---

### #FindTheBug (3)

**Duración:** 15-20 minutos (10 individual + 5-10 grupal)  
Clona el proyecto de aquí (carpeta: `proyecto3`)

**Principales vulnerabilidades encontradas:**

- **A05 - Security Misconfiguration**: A pesar de usar dotenv, el secret de JWT no está oculto en variables de entorno.
- **A01 - Broken Access Control**: El token de sesión expira en 30 segundos, pero se puede seguir navegando a la ruta de perfil.
- **A01 - Broken Access Control**: Se puede ver el perfil de otras personas cambiando manualmente la URL.
- **A03 - Injection**: El Dao de usuarios de Mongo recibe parámetros para dinamizar la búsqueda, pero el controlador no valida el query del request, exponiendo datos sensibles.
- **A07 - Identification and Authentication Failures**: El sistema permite n intentos de login sin bloqueo, expuesto a ataques de fuerza bruta.

---

☕ **Break** – 10 minutos

---

## Documentación

### ¿Qué es documentar?

**Documentar** significa brindar suficiente información sobre algún proceso, con el fin de que sea lo suficientemente entendible para quien lo lea.

La documentación puede darse a nivel simple (comentarios en el código) o a nivel más complejo (herramientas de documentación para un aplicativo).

### ¿Por qué es importante documentar?

**Caso contextual:**  
Trabajas en una empresa de venta de muebles bajo pedido. Solo hay dos desarrolladores: Mauricio (encargado de gestión de muebles y compras) y tú (usuarios, CRM, CMS).  
Mauricio renuncia y te quedas con un código que no hiciste y del que no tienes conocimiento.  
Te solicitan modificar el proceso de órdenes de venta para agregar órdenes premium.

**Problemas al enfrentarte al código:**

- No hay comentarios que guíen.
- No hay recursos que expliquen el flujo o puntos críticos.
- No hay ejemplos de input de `salesOrders` ni `purchaseOrders`.

**Conclusión:**  
Un código no documentado es una bomba de tiempo. Dificulta el mantenimiento, incluso para el autor después de meses.

---

## Swagger - https://swagger.io/

### ¿Qué es Swagger?

**Swagger** es una herramienta de documentación de código que permite mantener cada módulo de nuestra API dentro de un contexto con suficiente información para poder ser entendido por otros desarrolladores, o para nuestra versión del futuro. Con ella podemos crear nuestra propia **OpenAPI Specification**.

**OpenAPI Specification** (antes conocida como Swagger Specification) es un formato de descripción de REST APIs, escrito en YAML o JSON. Permite profundizar sobre módulos, rutas, esquemas, parámetros, etc.

---

### Proyecto de ejemplo: Adoptme

Adoptme es un proyecto para adopción de mascotas con las siguientes características:

- **Sistema de usuarios**: obtener todos, obtener por id, crear, actualizar, eliminar.
- **Sistema de mascotas**: obtener todas, crear, actualizar, eliminar.
- **Sistema de adopciones**: obtener todas, obtener por id, crear adopción (usuario + mascota).

Puedes clonar el proyecto [aquí]. No maneja entornos; solo reemplazar la URL de MongoDB en `app.js`.

---

### Instalación de Swagger

```bash
npm i swagger-jsdoc
npm install swagger-jsdoc swagger-ui-express
```

### Configuración inicial

```js
// swaggerOptions.js
const swaggerJsdoc = require("swagger-jsdoc");

const swaggerOptions = {
  definition: {
    openapi: "3.0.0",
    info: {
      title: "API Adoptme",
      description: "Documentación de la API para adopción de mascotas",
      version: "1.0.0",
    },
  },
  apis: ["./docs/**/*.yaml"], // ruta a los archivos de documentación
};

const specs = swaggerJsdoc(options);
module.exports = specs;
```

### conectamos Swagger a Express:

```js
const express = require("express");
const swaggerUi = require("swagger-ui-express");
const swaggerSpec = require("./swaggerOptions");

const app = express();

app.use("/api-docs", swaggerUi.serve, swaggerUi.setup(swaggerSpec));

// ... resto de middlewares y rutas
```

#### La documentación mínima debe incluir:

- Schemas
- Routes
- Inputs
- Responses

#### Estructura de carpetas

docs/
users.yaml
pets.yaml
adoptions.yaml

#### Definición de rutas (paths)

```yml
# docs/users.yaml
paths:
  /api/users/:
    get:
      summary: Obtiene todos los usuarios
      tags:
        - Users
      responses:
        "200":
          description: Lista de usuarios obtenida exitosamente
        "500":
          description: Error interno del servidor
```

#### Try it out

Swagger UI incluye un botón Try it out que permite ejecutar la petición directamente desde la interfaz.

#### generando componentes (schemas)

```yml
components:
  schemas:
    User:
      type: object
      properties:
        _id:
          type: string
          description: ID autogenerado de MongoDB
        first_name:
          type: string
          description: Nombre del usuario
        last_name:
          type: string
          description: Apellido del usuario
        email:
          type: string
          description: Correo electrónico
        password:
          type: string
          description: Contraseña (hasheada)
      example:
        _id: "60d21b4667d0d8992e610c85"
        first_name: "Juan"
        last_name: "Perez"
        email: "juan@example.com"
        password: "$2b$10$..."
```

#### Referenciar esquemas en respuestas

```yml
paths:
  /api/users/:
    get:
      responses:
        "200":
          description: Lista de usuarios
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
```

#### Parámetros de ruta

```yml
paths:
  /api/users/{uid}:
    get:
      summary: Obtiene un usuario por ID
      parameters:
        - name: uid
          in: path
          required: true
          description: ID del usuario
          schema:
            type: string
```

#### Definir el req.body (requestBody)

```yml
components:
  requestBodies:
    updateUser:
      description: Objeto con los datos a actualizar
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              first_name:
                type: string
              last_name:
                type: string
              email:
                type: string
```

#### Y luego referenciarlo en el endpoint PUT:

```yml
paths:
  /api/users/{uid}:
    put:
      summary: Actualiza un usuario
      parameters:
        - name: uid
          in: path
          required: true
          schema:
            type: string
      requestBody:
        $ref: "#/components/requestBodies/updateUser"
      responses:
        "200":
          description: Usuario actualizado
```

Objetivo: Documentar en Swagger el proceso de adopción.

Definir la documentación para mascotas (GET y POST).

Definir la documentación para el método register de usuarios (solo registro, no login).

Definir la documentación para adopciones (debe recibir doble parámetro: usuario y mascota).

Corroborar en la base de datos que las entidades se crean correctamente.

# Recursos
https://youtu.be/4p6z6hL8BNg?si=-HgPxdjVg3hZcM9K

! Terminar de agregar ejemplos de OWASP
! revisar array $ref