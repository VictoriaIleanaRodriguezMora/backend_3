components:
  parameters:
    schemas: # acá va el esquema que voy a crear para el registro de lo que yo me esté refiriendo Usuario/Mascota/Persona lo que yo quiera especificar
      MyUsers: # este nombre lo elijo YO, puede ser Users/MASCOTAS/pets/mypets, lo que sea
        type: object # para indicar que es un {}
        required: # lo saco del schema
          - first_name
          - last_name
          - email
          - password
          - role
          - pets??
        properties: # indico qué tipo de dato tiene cada campo
          first_name:
            type: string
            description: Nombre del usuario
            example: "Fulano mengano"
          last_name:
            type: string
            description: Apellido del usuario
          email:
            type: string
            description: Email del usuario
          password:
            type: string
            description: Contraseña hasheada del usuario
          role:
            type: string
            description: Rol del usuario
          pets: # ??
            type: string
            description: Apellido del usuario
      MyOtraCosa: # a la misma altura de MyUser, puedo definir el schema de lo que se deve enviar en la requets, de otra entidad de mi aplicación